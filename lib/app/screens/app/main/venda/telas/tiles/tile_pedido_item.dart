import 'package:flutter/material.dart';

import '../moddels/pedido_item.dart';

class TilePedidoitem extends StatelessWidget {
  const TilePedidoitem({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text(''),
    );
  }
}
